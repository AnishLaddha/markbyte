{
  "name": "markbyte-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for MarkByte",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  }
} 